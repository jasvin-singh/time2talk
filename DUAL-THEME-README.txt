TIME 2 TALK — COMBINED LIGHT + DARK WEBSITE

"
"Root / = Light Book Studio editorial theme (canonical/indexable)
"
"/dark/ = Dark theme version (noindex, canonical points to root equivalents)

"
"A split circular theme selector is in the header.
"
"Left half = Light theme
"
"Right half = Dark theme
"
"Switching themes opens the corresponding page, and normal navigation then stays inside that theme.

"
"All large images/audio/video are shared from the root /images/ folder so the ZIP does not duplicate media.
"
"Upload the full folder contents to the root of the existing GitHub Pages repository.
